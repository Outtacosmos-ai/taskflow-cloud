apiVersion: apps/v1
kind: Deployment
metadata:
  name: taskflow-worker
  namespace: taskflow
spec:
  replicas: 1
  selector:
    matchLabels:
      app: taskflow-worker
  template:
    metadata:
      labels:
        app: taskflow-worker
    spec:
      serviceAccountName: taskflow-sa # Added this so worker can read SQS
      containers:
      - name: worker
        image: WORKER_IMAGE # Placeholder for Kustomize
        env:
        - name: NODE_ENV
          value: "production"
        - name: MONGODB_URI
          value: "mongodb://mongodb:27017/taskflow"
        - name: SQS_QUEUE_URL
          value: "https://sqs.us-east-1.amazonaws.com/657577038059/taskflow-notifications-dev"
        - name: AWS_REGION
          value: "us-east-1"
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "200m"
            memory: "256Mi"
        livenessProbe:
          exec:
            command: ["pgrep", "node"]
          initialDelaySeconds: 30